<%= form_with(model: coin) do |form| %>
  <% if coin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coin.errors.count, "error") %> prohibited this coin from being saved:</h2>

      <ul>
        <% coin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container border rounded shadow">
  <div class="field">
    <%= form.text_field :description, class: 'rounded', placeholder:'test' %>
  </div>

  <div class="field">
    <%= form.text_field :acromym, class: 'rounded', placeholder:'test' %>
  </div>

  <div class="field">
    <%= form.text_field :url_image, class: 'rounded', placeholder:'test' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  </div>
<% end %>
