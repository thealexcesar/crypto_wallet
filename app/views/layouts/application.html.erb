<!DOCTYPE html>
<html>
  <head>
    <title>Crypto Wallet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path("hive-brands.svg"), class: 'color-icon' %>
  </head>

  <light
  <body>
    <header class="d-flex justify-content-around align-items-center text-center bg-light shadow">
      <%= link_to 'Crypto Wallet',root_path %>
      <h1 class="h4">Cryptocurrency Page</h1>
      <ul class="d-flex justify-content-between">
        <li>
          <%= link_to coins_url do %>
            <button class="btn btn-primary ">Coin registration</button></li>
          <% end %>
      </ul>
    </header>
      <div class="container-fluid m-2">
        <span class="float-end">Date: <%= date_us(Date.today)%></span>
      </div>
    <%= yield %>
  </body>
</html>
